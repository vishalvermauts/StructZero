version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: ide-architect-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis-data:
